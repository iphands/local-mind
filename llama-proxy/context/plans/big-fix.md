# big-fix — Remediation plan for the full-codebase audit (110+ findings)

**Status:** decision-complete; approval authorizes execution by a worker session (e.g. `/start-work`), never by this planning session.

**Artifact note:** the user requested `context/plans/big-fix.md`; the planner's write guard restricts planner writes to `.omo/*.md`. This file is the authoritative plan; task 100 (run FIRST by the worker) copies it verbatim to `context/plans/big-fix.md` so it sits beside the existing sibling plans.

**Provenance:** 2026-06 function-by-function audit of all 30 files / 18,060 LOC under `src/`, run by 6 parallel subagents. Saved reports (authoritative finding text; cited `[A]` stats/exporters/api, `[B]` fixes, `[C]` proxy core, `[D]` streaming/synthesis, `[E]` backends, `[F]` config/main/augment):

- `[A]` /home/iphands/.local/share/opencode/tool-output/tool_0c2539410001A7SoEPwkdcOVXH
- `[B]` /home/iphands/.local/share/opencode/tool-output/tool_0c24cd611001b3uDXREb0Ci1Um
- `[C]` /home/iphands/.local/share/opencode/tool-output/tool_0c253658c001dYIvfLoirtR7hN
- `[D]` /home/iphands/.local/share/opencode/tool-output/tool_0c2536745001cpwXC9e5hZ6b9e
- `[E]` /home/iphands/.local/share/opencode/tool-output/tool_0c253925e001peFSXr0T7tyLS9
- `[F]` /home/iphands/.local/share/opencode/tool-output/tool_0c253bd6c00113c8cqNgnivuIs

## Hard constraints (every task, non-negotiable)

1. **OpenAI compatibility contract (AGENTS.md):** preserve standard fields verbatim, tool_call ids/indices/order, finish_reason values, usage token counts (incl. extended details), SSE chunk framing, unknown-field pass-through both directions, Authorization behavior. Any task that could touch these ships with a compatibility regression test.
2. **Delta-safety rule:** nothing may reintroduce full-payload emission into an SSE delta stream. The only allowed stream mutations are per-fix, boundary-safe completions.
3. **Every phase is one green commit:** `cargo build && cargo clippy --all-targets -- -D warnings && cargo test` pass before moving on. Rollback = revert that single commit.
4. **No new abstractions beyond those specified.** No renames beyond specified. No new dependencies except `unicode-width` (D12).

## Adopted decisions (D1–D12) — the executor has no interview context; these ARE the rulings

- **D1** The legacy streaming fix machinery (streaming overrides on the real trait methods, `ToolCallAccumulator`, `calculate_completion_delta`, registry streaming methods, their tests) is **deleted, not repaired** — dead in the production path (streaming.rs consumes buffered `apply()`; oracle-verified: only test-module consumers exist), and six findings exist only because it is there. Grep gate in task 29 is the kill switch. NOTE: the tree ALREADY has `FixAction::Failed` (`fixes/mod.rs:42-45`, fields `original_snippet`/`attempted_fix`) and real context methods `applies_with_context(&Value, &Value)` / `apply_with_context(Value, &Value)` — no fictional `FixContext` type; reuse what exists.
- **D2** Dead config keys are **deleted, not implemented**: `stats.log_interval`, the whole `detection:` section, `exporters.influxdb.batch_size`/`flush_interval_seconds` (with README's false batching claim removed).
- **D3** `exporters.influxdb.enabled: true` without the `influxdb` cargo feature = **startup hard error**, not a silent drop.
- **D4** `#[serde(deny_unknown_fields)]` on every config struct **except** the `fixes.modules` map (intentional catch-all keyed by module name; also incompatible with serde flatten).
- **D5** Error statuses: no-backend → `503`; augment-backend failure → `502`; body-parse failure → `400`; over-limit body → `413`. All JSON errors use one envelope: `{"error": {"type": "...", "message": "..."}}`.
- **D6** `forward_response()` **does not exist** (oracle-verified: 0 grep hits). Task 9 EXTRACTS it — from the `proxy_passthrough` response-header loop — and routes both the buffered response builder and the passthrough branch through the single new helper.
- **D7** Decompression: `deflate` = zlib with raw-deflate fallback; brotli/zstd/deflate decode runs in `spawn_blocking`; hard cap 64 MiB decompressed (`MAX_DECOMPRESSED_BYTES`).
- **D8** `augment.rs` is rewritten to inject into `serde_json::Value` — no typed round-trip, unknown fields preserved by construction. `augment.enabled` defaults to **false**.
- **D9** `BackendGuard` becomes an `Arc`-shared permit (clonable; the LAST handle dropping releases the slot) and is moved into the streaming body future. `priority_free` claims slots with `compare_exchange`, never check-then-increment.
- **D10** Anthropic synthesis honors real streaming: a new `synthesis:` config section (`chunk_delay_ms` default **0**, `chunk_size_chars` default 2000) replaces the hard-coded **50 chars / 50 ms** at `src/proxy/synthesis.rs:24,28` (:24 = DEFAULT_CHUNK_SIZE 50 chars, :28 = DEFAULT_CHUNK_DELAY_MS 50 ms — pairing corrected oracle round 2; NOT in src/augment/). The `streaming:` key stays a plain mode string (`streaming: fake`) — no YAML shape break; README updated to match.
- **D11** Graceful shutdown on SIGINT/SIGTERM: `with_graceful_shutdown`, in-flight requests drain, **then** `flush_all()` + `shutdown_all()` run.
- **D12** Exactly one new dependency: `unicode-width` for CJK column alignment in `format_pretty`.

## Must-NOT-Have

No new features, endpoints, or CLI flags beyond those listed. No behavior changes beyond listed ones. No refactors outside listed split boundaries. No test deletion except where a task says so (task 29). No CI/CD changes. No changes under `vendor/` or `../context/`. No `server.max_body_bytes` (body-limit configuration stays out of scope).

## Test strategy

Tests-after with regression-first ordering per task: each fix task adds the failing-behavior test first (or documents why an existing test covers it), then the fix. All QA is agent-executable (`cargo test`, `cargo clippy`, `curl` against a live proxy, a fake-backend script) — zero human-intervention steps. `F`-wave tasks run the full matrix with evidence captured to `.omo/evidence/big-fix/`.

---

## Todos

**COMMIT ORDER IS AUTHORITATIVE from the "Commit order" section at the bottom — task numbers are stable IDs, NOT commit order.** Where that list puts a later number first (31 before 22/26/27/29), commit it first; every commit stays green (hard constraint 3).

### Phase 0 — Publish (run first)

- [x] 100. **Publish the plan to the user-requested path.** Copy `.omo/plans/big-fix.md` verbatim to `context/plans/big-fix.md` (directory exists) and commit as the first commit: `docs: add big-fix remediation plan`. Everything below/above stays byte-identical; this task exists because the planner's write guard cannot touch paths outside `.omo/`.

### Phase 1 — Character-safety class (CRITICAL + same-class) — closes [A-C1], [B-H2], [B-L7]

- [x] 1. **src/stats/request_log.rs:104-106 — rewrite `truncate_message` char-boundary-safe** (CRITICAL [A-C1]). Keep the >100-byte trigger; walk `char_indices()` to the largest boundary ≤ 25 for the head and the smallest boundary ≥ `len-75` for the tail. Sketch:
  ```rust
  if msg.len() <= 100 { return msg.to_string(); }
  let mut head_end = 0;
  for (i, _) in msg.char_indices() { if i > 25 { break } head_end = i; }
  let mut tail_start = msg.len();
  for (i, _) in msg.char_indices().rev() { if i >= msg.len() - 75 { tail_start = i } else { break } }
  format!("{} ... {}", &msg[..head_end], &msg[tail_start..])
  ```
  ASCII output must stay byte-identical to today (existing tests pin it). Test: `truncate_message_cjk_no_panic` — 12× U+4E00 (36 bytes) + 200 ASCII so byte 25 lands mid-char; assert both slices land on char boundaries. Accept: new test + existing suite green.
- [x] 2. **src/fixes — one shared char-safe snippet helper** [B-H2, B-L7]. The char-safe `truncate_snippet` ALREADY EXISTS at `src/fixes/registry.rs:16` (oracle-corrected ref) — widen it to `pub(crate)`; delete the byte-slicing duplicates at `src/fixes/mod.rs:136-142` and `src/fixes/toolcall_bad_filepath_fix.rs:71-77`, replacing call sites with the registry helper. Accept: `create_snippet` gone; grep shows exactly one truncator in fixes/.
- [x] 3. **src/fixes/toolcall_bad_filepath_fix.rs — multibyte regression tests for the buffered fix path** [B-H2]. Feed `{"filePath":"/x/中文测试目录/file.rs","content":"..."}` payloads through `apply()`; assert no panic + correct output. Accept: green with and without task 1.

### Phase 2 — HTTP response contract — closes [C-H1, C-H2, C-H3, C-M4, C-M5, C-M10, C-L4, C-L5, C-L6, C-L8]

- [x] 4. **src/proxy/handler.rs:326-333, 500-507 — return real error statuses** [C-H1, D5]. No-backend-configured → `(StatusCode::SERVICE_UNAVAILABLE, Json(err_envelope("no_backend", msg))).into_response()`; augment failure → `StatusCode::BAD_GATEWAY` / `err_envelope("augment_backend_error", ...)`. Add helper `fn err_envelope(kind: &str, msg: impl Into<String>) -> serde_json::Value` producing `{"error":{"type":kind,"message":msg}}`. Tests: status assertions on both paths via a no-backend state and a dead augment URL.
- [x] 5. **src/proxy/handler.rs — uniform JSON error envelope** [C-L5, C-L6, D5]. Convert the plain-text `400`/`415`/`422` bodies (lines ~288, 302, 392, 420, 601, 853) to `err_envelope` with `type` ∈ {`invalid_request_json`, `unsupported_media_type`, `anthropic_format_error`, ...}. Keep human messages inside `message`. When the RESPONSE already carries a JSON media type, preserve its original `Content-Type` value verbatim (charset included) instead of re-emitting bare `application/json` [C-L6].
- [x] 6. **src/proxy/handler.rs:302-306, 1168-1171 — map body-reader errors by kind** [C-L4, D5]. Body length-limit errors → `413`, other read/parse errors → `400`, both via `err_envelope`. Fact check (oracle round 2): there is NO `DefaultBodyLimit` layer — caps are hard-coded `to_bytes` limits: 100 MiB main path (`handler.rs:302`, `Err` arm :304-306), 10 MiB passthrough (`:1168`, `Err` arm :1169-1171). The method `is_length_limit()` does NOT exist in axum 0.7.9/axum-core 0.4.5 (0 grep hits; earlier wording fictional). Classification primitive: the `to_bytes` error surfaces `http_body_util::LengthLimitError` inside a BoxError on over-limit — match via Display (`e.to_string() == "length limit exceeded"` — lowercase, the exact Display of http-body-util-0.1.3 `limited.rs:97` surfaced through axum_core's transparent error wrapper, oracle round-3 verified; NO `http-body-util` direct dep needed, hard constraint 4 intact); pin with an oversized-body test. Making the cap configurable is Must-NOT-Have.
- [x] 7. **src/proxy/handler.rs:1138-1151, 1243-1252 — strip Content-Encoding only when the body was actually transformed** [C-H2]. Change `decompress_body` return to `enum Decompressed { Passthrough(Vec<u8>), Decoded(Vec<u8>) }`; in the header copy loop skip `CONTENT_ENCODING` only on `Decoded`. Test: fake backend sends `Content-Encoding: gzip` + gzipped body through the synthesis path → no `content-encoding` header out, body parses.
- [x] 8. **src/proxy/handler.rs:168-179 — deflate alias correctness** [C-M4, D7]. For `deflate`: `ZlibDecoder` first; on decode error retry `DeflateDecoder` (raw); one `debug!` on fallback. Acceptance: unit tests with zlib and raw-deflate fixtures both decode.
- [x] 9. **src/proxy/handler.rs — EXTRACT `forward_response()` and route both response-copy sites through it** [C-H3, D6]. The helper does NOT exist yet (oracle-verified 0 hits). Extract it from the `proxy_passthrough` response-header loop (`handler.rs:1242-1252` — oracle round 2: the loop today skips only Content-Length/Transfer-Encoding; the extracted helper ALSO strips Content-Encoding per task 7 and hop-by-hop set; the request-side Accept-Encoding skip at :757 stays put), give it the signature `fn forward_response(resp: reqwest::Response, status: StatusCode) -> Response`, then call it from BOTH passthrough branches (replacing the hand-rolled loop at ~1188-1206 — the third Content-Encoding leak site) and the 503/429 arms that today duplicate the loop. Accept: one response-header-copy implementation; hop-by-hop predicate exists exactly once.
- [x] 10. **src/proxy/handler.rs:141-212 — offload + cap decompression** [C-M5, D7]. brotli/zstd/deflate decode via `tokio::task::spawn_blocking`; each decoder chain wrapped in `.take(MAX_DECOMPRESSED_BYTES + 1)` (module const, 64 MiB); over cap → `err_envelope("backend_payload_too_large")`. Acceptance: gzip-bomb fixture errors cleanly, no OOM, async reactor unblocked during decode (test with a concurrent lightweight request).
- [x] 11. **src/proxy/handler.rs:1056-1125 — synthesis failure must not 200-then-passthrough** [C-L8]. On `synthesize_anthropic_response` error or `convert_openai_to_claude_sse` error for an Anthropic-format request (the silent arm at ~1074-1078): emit SSE `event: error` frame + `data: [DONE]` and end. Acceptance: test asserts error frame present, no raw OpenAI body leaked.
- [x] 12. **src/proxy/handler.rs:1017-1049 — dump records what the client received** [C-M10]. Dump the ORIGINAL pre-decompression bytes with the ORIGINAL `Content-Type` captured before transformation; if transformed, also write the decoded variant with `.decoded` suffix. Test: gzipped fake response → dump file byte-equal to original gzip bytes.

### Phase 3 — Routing correctness — closes [C-H4, C-H5, C-M8, C-L1]

- [x] 13. **src/proxy/handler.rs:283-344 — route on the REQUESTED path** [C-H4]. After backend selection (~319) compute `let routed = backend.node.effective_path(path);` and run the route match (337-444) against `routed` (anthropic detection checks original + routed). Predicate: `starts_with("/completions") || contains("/chat/completions") || contains("/v1/messages")`. Tests: node `path_prefix: "/completions"` + request `/completions/v1/messages` → synthesis path; plain `/v1/chat/completions` unchanged.
- [x] 14. **src/proxy/handler.rs:767-794 — gate pass-through injections on completion shape** [C-H5]. New `fn looks_like_completion_request(body: &Value, path: &str) -> bool` (completion-shaped path OR non-empty `messages` array). `apply_backend_overrides_bytes(&body, backend, path)` takes path; inject `stream:false` / `stream_options` / `model` / `temperature` ONLY when true. Tests: POST to a non-completion passthrough route keeps body byte-identical; chat route still gets injection.
- [x] 15. **src/proxy/context.rs:106-111, 136-141 — delete the prefix-strip blocks** [C-M8]. Monitoring calls backend-native `/props` and `/v1/models`; the strip mangles prefix-configured nodes (`/v1/v1/models`). Acceptance: test with `path_prefix="/completions"` asserts request URI suffix is exactly `/props`.
- [x] 16. **src/proxy/handler.rs:341-343 — delete dead `/health` match arm** [C-L1]. Server-level `/health` (server.rs:231) is live; post-delete smoke: `curl /health` returns the server body.

### Phase 4 — Load balancing & backend clients — closes [E-H1, E-H2, E-M1, E-M7, E-L1..L5]

- [x] 17. **src/backends/priority_free.rs — atomic claim** [E-H2, D9]. Counter is `AtomicUsize` (NOT u64) at `node.rs:16`. Free claim via `compare_exchange(0, 1, AcqRel, Acquire)` per candidate (works on the inline field — no Arc needed yet); all-busy path: `fetch_add(1)` each candidate, track min-load best, `fetch_sub(1)` on every replaced candidate (increment-then-backtrack; no unclaimed window; oracle-verified: winner never decremented, no double-decrement). Locate code by symbol — the claim site is `priority_free.rs:31-34` + `balancer.rs:20`. Test: 8 concurrent selects against one idle node → exactly one free-claim winner.
- [x] 18. **src/backends/balancer.rs:12-38 + src/proxy/handler.rs:1063, 1085 — permit-style guard into the stream body** [E-H1, D9]. PRECONDITION (oracle): `BackendNode.active_requests` is an INLINE `AtomicUsize` (`node.rs:16`, constructed `:40`, test struct literals e.g. `priority_free.rs:67`) — convert the field to `Arc<AtomicUsize>` and update every construction/struct-literal site FIRST. Then:
  ```rust
  struct BusyHandle(Arc<AtomicUsize>);           // Drop: fetch_sub(1, Release)
  #[derive(Clone)]
  pub struct BackendGuard { busy: Arc<BusyHandle>, pub node: Arc<BackendNode>, pub group_name: Option<String> }
  ```
  Claim wraps the incremented counter in ONE `BusyHandle`; clones share it; decrement exactly once when the last handle drops. Precedent that a `'static` moved handle works in the body future: streaming.rs already parks `Option<OwnedSemaphorePermit>` for body lifetime (`:543, :567, :609`). `handle_streaming_response` gains a `guard: BackendGuard` param, moved into the boxed body future (`let _guard = guard;` first statement). Test: counter stays 1 while the body future is pending, hits 0 after body completion AND after mid-stream drop.
- [x] 19. **src/backends/node.rs:62 — client timeouts** [E-M7]. Add `.connect_timeout(Duration::from_secs(5))` in `build_node_client` (oracle-corrected name); same builder reused by preflight (task 85). Builder unit test asserts the setting.
- [ ] 20. **src/backends — LOW cluster** [E-L1..L5]. `priority_free.rs` `unwrap` → `expect("at least one candidate")`; delete dead `with_group` (`balancer.rs:25-31`, oracle-corrected range); shared `candidate_order()` helper so all strategies iterate identically; `debug_assert!(counter > 0)` in the guard-drop path.
- [x] 21. **src/backends — runtime failure marking + cooldown** [E-M1]. `BackendNode` gains `healthy: AtomicBool` + `cooldown_until: Mutex<Instant>`. Strategy `select()` skips cooled-down nodes; if ALL cooled, pick soonest-expiry with `warn!`. Wire `mark_failed(Duration)` on connect error / 429 (extend handler.rs:438-441) / 5xx; clear on success. Config: per-group `failure_cooldown_secs: u64` (default 30) on `BackendGroupConfig` — oracle-corrected placement: `backends:` is a HashMap alias (`config/mod.rs:185`) with no scalar slot; the struct is deny-compatible. Acceptance: fake node 502s twice → skipped on third select until injected clock advance; success clears.

### Phase 5 — Fixes layer behavior — closes [B-H1, B-H3, B-H4, B-M1..M6, B-L1..L4]

- [x] 22. **src/fixes/toolcall_bad_filepath_fix.rs:92-116 + src/fixes/registry.rs:64-77, 94-107 — fail-safe on fixer errors** [B-H1]. Post-task-31 interface: fixer helpers return `Result<_, FixError>`; on `Err` the registry logs `FixAction::Failed` — the variant ALREADY EXISTS (`fixes/mod.rs:42-45`, fields `original_snippet`/`attempted_fix`; oracle: do NOT add a variant — carry the reason string in `attempted_fix`) — and the response keeps the ORIGINAL `arguments`, never `"{}"`. Acceptance: unparseable-but-triggering payload → output == input, one `Failed` log record, no panic.
- [x] 23. **src/fixes/json_scan.rs (new) — structural JSON-key scanner** [B-H4, B-M1]. `pub(crate) fn top_level_key_count(json: &str, key: &str) -> usize` via `serde_json::de::Deserializer` + custom `Visitor` walking `MapAccess` at depth 1, counting key occurrences with values deserialized as `serde::de::IgnoredAny`. Oracle-corrected mechanism: duplicate-key collapse happens ONLY inside serde_json's `Value` MapAccumulator (`value/de.rs:136-144`), NOT in the parser stream — an `IgnoredAny` walk sees every entry. PIN THIS PREMISE with a unit test: `{"filePath":1,"filePath":2}` must count 2 (if the premise ever fails on our serde_json version, fall back to a brace-depth-aware raw-text scan — same signature). Also expose ordered key/position capture for first-wins rebuild. ~60 LOC + tests: counted at depth 1; `"filePath"` ONLY inside a string value returns 0.
- [x] 24. **toolcall_bad_filepath_fix.rs:49-63 — is_malformed via the scanner** [B-M1, B-H4]. `is_malformed` = invalid JSON OR `top_level_key_count(args,"filePath") > 1`. Delete the substring-count heuristic and the `find("\"filePath\":")` positional check. Acceptance: `content` containing literal `{"filePath":` does NOT trigger; real duplicate-key payload DOES.
- [x] 25. **toolcall_bad_filepath_fix.rs:85-124 — unify first-wins** [B-M2]. Buffered path, when duplicates exist: rebuild via a SECOND parse pass over the raw string using the task-23 scanner's ordered positions, keeping the FIRST `filePath` value — same winner as the streaming accumulator documented at :92-105. Acceptance: one fixture, buffered output's `filePath` == documented streaming winner.
- [x] 26. **src/fixes/toolcall_malformed_arguments_fix.rs:86-126 — positional replacement** [B-H3, B-M4]. Collect missing schema keys once; replace `{"": ...}` occurrences IN ORDER via `Captures::iter` splicing — occurrence N gets missing key N, leftovers untouched (then fail validation → task 22 fallback preserves original). Acceptance: two-empty-keys test assigns distinct keys in order.
- [x] 27. **toolcall_malformed_arguments_fix.rs:136-193 — no-guess parsing** [B-M5]. Candidates = (schema keys) − (keys present per task-23 scanner). Exactly one candidate → use; otherwise `Err` (original preserved via task 22). Regex positional-guessing block deleted. → ROOT-ADJUDICATED (Oracle; fix-forward 27e1222): gate is a BIJECTION — candidates == slots → ordered splice (restores task-26's B-H3 acceptance: Qwen3-Coder slot values follow schema order by construction); candidates > slots (incl. 1-slot/N-candidate) or == 0 → `Err` — subset selection remains the condemned guess class; regex-guesser deletion + structured masked-scanner presence detection stand.
- [x] 28. **toolcall_malformed_arguments_fix.rs:289-321 — applies() over all choices + deltas** [B-M6]. Mirror null_index's delta handling; iterate every choice's `message` AND `delta`; `apply()` symmetric. Acceptance: malformed call in `choices[1]` triggers the fix.
- [x] 29. **Delete legacy streaming machinery (trait + accumulator + registry + tests)** [B-M7, B-M8, B-L5, B-L6, B-L8, D1]. GATE FIRST: `rg -n "apply_fixes_stream|apply_stream|ToolCallAccumulator|calculate_completion_delta" src/` — ALLOWED hits: ONLY the deletion-set files below (oracle confirmed: prod code is dead; the real consumers are the `streaming.rs` TEST region). Any hit outside the set → STOP + flag in commit body. DELETE (locate by symbol; ranges are current-tree guides): trait streaming defaults (`fixes/mod.rs:196-246`), `ToolCallAccumulator` (`fixes/mod.rs:79-172`), `calculate_completion_delta` + `extract_tool_call_id` + the accumulation override (`toolcall_bad_filepath_fix.rs`, override `:299-330`), malformed-args streaming overrides (`toolcall_malformed_arguments_fix.rs:304, :327`), registry streaming methods (`registry.rs:79-157` — KEEP `:179-210`, the detect-only helper), registry streaming tests (`registry.rs:486-496`, `:679-713`), streaming.rs accumulator TEST region (`:1539-1750` inside `mod tests` @1308), the delta-machinery tests in `toolcall_bad_filepath_fix.rs` (`:1209, :1334, :1477`) — oracle round 2: KEEP `toolcall_bad_filepath_fix.rs:549` `test_fix_arguments_non_ascii_filepath`, buffered-path char-safety coverage, not streaming machinery. (Oracle-corrected: there is NO `FixContext` type; earlier ranges re-derived by symbol.) Buffered path + `check-config`/`list-fixes` MUST remain green.
- [ ] 30. **src/fixes — residual LOWs** [B-L1..L4, B-M3]. null_index: mutate `tool_calls` in place (remove clone-then-reassign :120-144); delete the dead internal `enabled` flag (registry owns enabled state — `set_enabled` never wired) [B-L3]; document index-repair semantics; registry config-name normalization already accepts suffixed/unsuffixed — keep. registry: collect per-call `FixAction`s into `Vec` and emit ONE aggregated line per fix (kills the `overall_action` overwrite). bad_filepath: module doc stating the `filePath`-schema coupling is by-design config domain [B-L1].

### Phase 6 — Fix registry interface & config wiring — closes [B-H1 enabler, F-M9 partial]

- [x] 31. **src/fixes/mod.rs + registry.rs — Result-typed fail-safe interface** (commit FIRST in the fixes cluster; enables 22/26/27/29). Oracle-corrected against the REAL trait (`fixes/mod.rs:184-246`): the existing methods are `applies_with_context(&self, response: &Value, request: &Value) -> bool` and `apply_with_context(&self, response: Value, request: &Value) -> (Value, FixAction)` — overrides live in BOTH fix files; callers are `registry.rs:64-77` (`apply_fixes`, call site :69) and `registry.rs:94-107` (`apply_fixes_with_context`, call site :99 — recited oracle round 2/3), plus the detect-only site `detect_fixes` (`registry.rs:172-212`). Changes: add `pub enum FixOutcome { NotApplicable, Fixed }` + `pub enum FixError { Parse(String), Rebuild(String) }`; change `apply_with_context` to return `Result<(Value, FixAction), FixError>` with trait default `Ok((response, FixAction::NotApplicable))`; update both overrides and both registry call sites (`Err` → log the EXISTING `FixAction::Failed`, return response untouched). No `FixContext`, no `&mut ToolCall` — those were fictional. `apply()` stays the simple wrapper.
- [x] 32. **src/fixes/registry.rs:122-135 + src/main.rs — honor per-module `enabled`**. Build the registry from `config.fixes.modules`: only construct enabled modules / `disable(name)` disabled ones at startup. Acceptance: `{enabled: false}` in config → startup log + registry length test.
- [x] 33. **src/fixes/registry.rs — applies-gate doc** [B-L5]. Doc-comment the (now gone) streaming asymmetry resolution: buffered path gates on `applies()` per trait contract; one short note.
- [x] 34. **src/main.rs list-fixes — real enabled state** [F-M9]: load config when `--config` given; print enabled/disabled per module from the CONSTRUCTED registry (not hardcoded true). Pairs with 32; final CLI behavior lands with task 75.

### Phase 7 — API types serde hygiene — closes [A-H1, A-M1..M7, A-L1..L5]

- [x] 35. **src/api/openai.rs:21-56, 62-63 — `ChatCompletionRequest` Option fields**: `#[serde(skip_serializing_if = "Option::is_none")]` on ALL Option fields (`tools`, `tool_choice`, `stream`, `max_tokens`, ..., `Message.content`). Acceptance: parse `{model, messages}` → re-serialize contains NO null keys.
- [x] 36. **src/api/openai.rs:8-17, 184-201 — arguments + ToolCall null hygiene** [A-M2, A-L1, A-L2]. `deserialize_arguments`: `Value::Null` → `"{}"` (not `"null"`); `FunctionCall.arguments` `#[serde(default = "default_empty_args")]` (`"{}"`); `ToolCall`: add `skip_serializing_if` to `id`, `r#type` (call_type) AND `index` — all `Option` today; report A M2: synthesis embeds the `Vec<ToolCall>` verbatim (`synthesis.rs:127`) and strict clients reject null `id`/`type`. Acceptance: null-arguments fixture round-trips to `"{}"`; synthesis output contains no `"arguments":null` and no null `id`/`type`/`index` keys.
- [x] 37. **src/api/openai.rs:146-165 — response leniency** [A-M3, A-M4]. `Choice`: `skip_serializing_if` on ALL THREE of `message`/`delta`/`finish_reason` (report A M3; `message` also `#[serde(default)]`). `ChatCompletionResponse`: `#[serde(default)]` on `id`, `created`, `model`, `choices`; `object` default `"chat.completion"` — every currently-required field defaulted so a minimal `{}` parses. Acceptance: `{}` parses; real response round-trips.
- [x] 38. **src/api/openai.rs:338-353 — Anthropic request leniency** → ROOT-RATIFIED: shipped non-`Option` `#[serde(default)]` + `Default` (the `Option<AnthropicUsage>` literal breaks synthesis/handler compile outside the task's file scope; acceptance behavior identical, seam `From → Some(Usage{0,0,0})` read-verified). [A-M5]. `AnthropicMessage.content` `#[serde(default)]`; `AnthropicMessage.usage` → `Option<AnthropicUsage>` `#[serde(default)]`; `AnthropicUsage.input_tokens`/`output_tokens` `#[serde(default)]` (report A M5: required `usage` aborts the whole parse when absent).
- [x] 39. **src/api/openai.rs:353-377 — unknown Anthropic content blocks survive** [A-H1]. Manual `Deserialize` for `AnthropicContentBlock`: internal `Helper` enum for the 6 tagged shapes; unknown `type` (or shape mismatch) → `Other(serde_json::Value)` preserving the WHOLE raw object; Serialize emits `Other` verbatim. Conversion paths pass `Other` through into content arrays unchanged. Acceptance: request containing `{"type":"advisor_tool_result",...}` is forwarded structurally intact; known block tests unchanged.
- [x] 40. **src/api/openai.rs:396-419, 454 — thinking/ToolResult conversion** [A-M6, A-M7]. `thinking` block → `reasoning_text` (NOT `content`); map request `reasoning_text`/`reasoning_opaque` through (currently hardcoded `None`); `ToolResult.content` array-of-parts → join `text` fields. One conversion unit test per rule.
- [x] 41. **src/api/openai.rs:271-287 + src/api/llama.rs — latent LOWs** [A-L4, A-L5]. `StreamChunk.delta` `#[serde(default)]`; `SlotInfo.state` default `"unknown"`, `id`/`priority` `#[serde(default)]`; `ServerProps.n_ctx` `#[serde(default, flatten)]` fallback map; `ModelData.created` default 0. Acceptance: `/slots` fixture missing `state` parses (context.rs:117 unwrap no longer exposed).
- [x] 42. **src/stats/request_log.rs:48 — stop-on-parse-failure** [A-L9]. First user message that fails to parse → push `"<unparseable>"` and CONTINUE scanning; `None` only when no user messages exist.

### Phase 8 — Augment rewrite (D8) — closes [F-H3, F-M11..M15, F-L7..L9]

- [x] 43. **src/augment.rs — Value-based injection** → CARRY: handler:628 still calls the typed shim; one-line switch to `inject_augmentation_value` owed to the handler cluster for the runtime win. [F-H3, F-M15]. Replace the typed `ChatCompletionRequest` round-trip with `serde_json::Value` surgery: locate system message; append/replace augment block per current semantics (string concat for `content: String`, push/replace text part for array content); normalize `"stop": "<string>"` → `["<string>"]` BEFORE injection; re-serialize. Unknown fields at every level untouched BY CONSTRUCTION. Acceptance: request with unknown top-level + message-level fields → those exact keys byte-identical in the augmented output.
- [x] 44. **src/augment.rs:208-211 — no system-message clobbering** → adjudicated: real clobber site was the no-user fallback; push-at-tail `{role:system}` shipped; injection target remains LAST USER message (plan's system-message premise was stale). [F-M11]. No system message present → PUSH `{"role":"system","content":<augment>}`; never overwrite `messages[0]` (currently destroys the user's system prompt).
- [x] 45. **src/augment.rs:196-206 — Parts else-arm** [F-M12]. Last part non-text → APPEND a new text part; do not mutate `text: None` to `Some`.
- [x] 46. **src/config — augment opt-in + validated** [F-M13]. `AugmentBackendConfig.enabled` default **false**; loader rejects non-http(s) `augment.url` (validation code lands in task 71; this task flips the default).
- [x] 47. **src/augment.rs:47-57 — async fs + cache** [F-M14]. `tokio::fs` + mtime-checked cache; EXTRACT the existing reprompt mtime-cache pattern into one shared `PromptFileCache` (used by tasks 47 and 63 — single implementation, not copy-paste).
- [ ] 48. **src/augment.rs — error detail + timeout config** [F-L7, F-L8]. Error carries status + first 300 chars of body (full body at `debug!`); new `augment.timeout_secs` (default 15) replaces hard-coded 60 s.

### Phase 9 — Streaming engine — closes [D-M1..M7, D-L1..L13]

- [ ] 49. **src/proxy/streaming.rs:619-629 — remove per-request deep clones** [D-M6]. Rewritten (oracle round 2): the earlier mechanism citation (`accumulate_merge_fields` / `acc.clone()` at :1190/:1202) was fictional — 0 grep hits; the real concat at those lines is already covered by task 53(d). The ACTUAL [D] M6 finding: `backend_request_body` deep-cloned per request at :628 plus companion clones :619-629 (Arc clone :619, `request_json_for_dump` clone :629 — oracle round 3). Fix: MOVE the request body into the `async move` body future and gate remaining clones behind `dump_path.is_some()` (confirmed: `backend_request_body` is consumed again only inside the dump block at :783-795 — dump is the ONLY second consumer), so the default (no-dump) path clones nothing. Acceptance: streaming tests green; code inspection — the only surviving request-body clone is the dump-gated one.
- [ ] 50. **src/proxy/streaming.rs:631-873 — decompose the closure** [D-M7]. `struct StreamState { acc, current_tool_index, seen_indices, event_type, framer, synth_stats, ... }`; closure becomes `impl StreamState { fn on_sse_block(&mut self, ...) }`. Pure move — behavior identical, callers unchanged. Run the existing streaming tests BEFORE the refactor as the golden harness.
- [ ] 51. **src/proxy/streaming.rs:412-428, 271-276 — pay for what you use** [D-M2]. Skip per-line `serde_json::from_str` unless (a) stats/exporter listeners active, or (b) anthropic bridge active AND byte prefilter hits: `fn mentions_bridge_event(b: &[u8]) -> bool { b.windows(5).any(|w| w == b"[DONE") || b.windows(12).any(|w| w == b"message_stop") }`. Capture listener presence as `const`-like bool once outside the loop.
- [ ] 52. **src/proxy/streaming.rs:137-146 — CRLF framing** [D-M1]. `SseFramer::push` normalizes `\r\n` → `\n` and strips stray `\r` before the `\n\n` scan. Test: `"data: x\r\n\r\ndata: y\r\n\r\n"` yields exactly two events.
- [ ] 53. **src/proxy/streaming.rs:1170-1257 — accumulator correctness** [D-M3..M5, D-L4, D-L5]. (a) seed `acc["choices"] = []` on first chunk; (b) missing-index tool-call fragments merge into the last slot recorded for that choice instead of dropping; (c) late name fragments append to the same accumulated entry after index reassignment; (d) hot `format!("{}{}", ...)` concat sites become `push_str` via `get_mut` on `Value::String`. Unit test per sub-edit.
- [ ] 54. **src/proxy/streaming.rs (stats slice) → src/stats/collector.rs** [D-M7, A-M11 sibling]. Extract usage/timings accumulation as `pub fn collect_from_synth_stream(&mut SynthStats, &Value)`; `from_synth_stream` assembles at the end. Same data, new home.
- [ ] 55. **src/proxy/streaming.rs — LOW bundle 1 (framing/idiom)** [D-L7..L13]. ForceFlush appends `"\n\n"`; reset `event_type` at blank-line boundaries; event counting from `SseFramer` only (delete double count :141-149/:335); consts `MAX_MERGE_FIELDS: usize = 100`, `HEARTBEAT_INTERVAL_SECS: u64 = 15`; tidy the `let _permit` idiom at :198-201.
- [ ] 56. **src/proxy/streaming.rs — LOW bundle 2 (errors/logs)** [D-L1..L6]. Parse `unwrap`/`unwrap_or_default` → `unwrap_or_else` with logging (:210, :889); `drain_error` routed through the same emit-error path as normal errors; `stalled` classification only when `completion_rx` failed (client disconnect → `client_gone`); PROMOTE the swallowed-error logs currently at TRACE (:717, :866) to `debug!`, and to `warn!` when nothing was emitted (report [D] D-L1 asks for promotion — both reviewers caught the earlier reversed wording); dump failure → `warn!`; response headers cloned once per response (:546-548).

### Phase 10 — Anthropic synthesis parity — closes [D-M8, D-M9, D-L14..L22]

- [ ] 57. **src/proxy/handler.rs:96-126 + src/proxy/synthesis.rs:24,28 — config-driven chunk timing** [D-M8, D10]. Oracle-corrected path/facts: the file is `src/proxy/synthesis.rs` (NOT src/augment/); constants at `:24,:28` are **50 chars / 50 ms delay** (:24 = size, :28 = delay — pairing fixed round 2). Add a new config section `synthesis:` → `SynthesisConfig { chunk_delay_ms: u64 (default 0), chunk_size_chars: usize (default 2000) }` — the `streaming:` key STAYS a plain mode string (YAML shape preserved; the `StreamingConfig` type-alias is removed by task 73, this task only ADDS `SynthesisConfig`). Handler passes config into synthesis; hard-coded constants gone. Chunking stays CHAR-based (`chunk_text` is emoji-aware, tests `:566-597`) — hence `chunk_size_chars`. README `Streaming (Fake Mode)` section rewritten to match. Acceptance: with default config, inter-chunk delta < 5 ms (test with Instant); old constant references deleted.
- [ ] 58. **src/proxy/synthesis.rs:99-216 — envelope factory** [D-L14, D-L18]. One `fn sse_envelope(meta: &StreamMeta, event: &str, data: Value, idx: u64) -> Value` covering ALL call sites — six `create_sse_event` calls at :102/:119/:137/:154/:172/:211 (oracle round 3 count; the `[DONE]` push at :214 is not an envelope). (Oracle round 2: the previously listed `StreamingChunk` deletion is DROPPED from scope — no such struct exists; :25-29 is the doc-comment + delay constant that task 57 already removes.)
- [ ] 59. **synthesis.rs:116-167, 52 — ordering + honest finish_reason** [D-L15, D-L16]. Part order: `role` → thinking/reasoning deltas → text deltas → tool-call arg deltas → block stop. `finish_reason` derived (tool calls → `tool_use`; max tokens → `max_tokens`; else `end_turn`); stop fabricating `stop_sequences` for brace fragments.
- [ ] 60. **synthesis.rs:348-367, 389-410 — full part coverage** [D-L19, D-L20]. `ToolResult` blocks emitted as `tool_result` content blocks with gap-free indices; `signature`-bearing thinking emits `signature_delta`.
- [ ] 61. **synthesis.rs:37-82 — all choices + unknown preservation via RAW Value** [D-M9]. Concrete mechanism (momus: earlier wording was insufficient): synthesis must consume the **raw buffered `Value`** of the backend response (the fake pipeline holds it before any typed parse) and build the Anthropic envelope by reading `Value` pointers directly — typed `ChatCompletionResponse` deserialization is bypassed for envelope construction, so unknown top-level/response fields survive by construction; iterate ALL `choices` array entries (start/stop sets per choice index), not just `choices[0]`.
- [ ] 62. **synthesis.rs — infallibility + naming** [D-L21, D-L22]. `convert_openai_to_claude_sse` → `Vec<SseFrame>`; `synthesize_anthropic_response` → `Value` (doc why infallible); rename `chunk_text` → `split_chars`.

### Phase 11 — Reprompt engine — closes [C-M1, C-M2, C-M3, C-M6, C-M7, C-L15..L19]

- [ ] 63. **src/reprompt.rs:107-127, 236-250 — async fs + one atomic state** [C-M2, C-L17]. `tokio::fs`; single `RwLock<PromptState { text, mtime }>` snapshot per call (kills non-atomic double-lock and per-request stat); uses shared `PromptFileCache` from task 47.
- [ ] 64. **reprompt.rs:360-406 — time budget** [C-M3]. `reprompt.max_total_ms` (default 30_000): deadline checked before each round AND each follow-up POST wrapped in `tokio::time::timeout`; on expiry `finish()` + return last acceptable with `debug!`. Acceptance: slow-fake-backend test returns within budget.
- [ ] 65. **reprompt.rs:375-381 — continuation before sentinel consumption** [C-M7, C-L19]. Move `has_continuation` check BEFORE `take_sentinel_replacement`; on no-continuation restore the sentinel into the text. Test: non-continuation reprompt response keeps its sentinel in the returned text.
- [ ] 66. **reprompt.rs:422-434, 221 — finish() guarantees a message; propagate follow-up errors** [C-M6]. Last message missing or tool-calls-only → append assistant text (streamed-text accumulator, else `"Reprompt loop ended: no further text."`). `let _ =` on `finish()` → `?`/match with `warn!`. Acceptance: mid-loop tool_calls response no longer yields a stripped body downstream.
- [ ] 67. **reprompt.rs:303-321 — follow-up request fidelity** [C-L15, C-L16]. Build via `url::Url::join` on the node base + ROUTED path + original query string; `build_follow_up_request` returns `None` when extraction yields no messages → skip the round instead of re-POSTing an effectively-empty body.
- [ ] 68. **reprompt finish_reason helper + fixes/metrics on the reprompted body** [C-L18, C-M1]. Shared `pub fn anthropic_has_continuation(text: &str, sentinel: &str) -> bool` used by reprompt.rs:260-267 and streaming.rs. In handler.rs:919-935 — after a reprompt merge run `fix_registry.apply_fixes_with_context` on the merged response, and MOVE `RequestMetrics::from_response` (handler.rs:977+) to AFTER `maybe_reprompt` with the start instant moved up, so the ONE log/export line reflects merged totals. Acceptance: scripted fake backend, sentinel first response → single log line with merged completion_tokens/duration; fixes observed on merged tool call.

### Phase 12 — Config schema hardening — closes [F-H1, F-H2, F-M1..M6, F-M10, F-L1..L6]

- [ ] 69. **src/config/mod.rs:13 — `backend` optional** [F-H1]. Oracle-corrected: `backend: Option<BackendConfig>` + `#[serde(default)]` (→ `None`). `BackendConfig`'s REAL fields are `url: String`, `timeout_seconds`, `tls`, `model`, `api_key`, `strip_path_prefix` (`config/mod.rs:81-102`) — there are NO host/port fields (the earlier host/port Default sketch was AGENTS.md drift; hand-writing `Default` for a non-Option would also not compile, hence the Option). Use-site fallback: `None` → group routing if `backends` present, else the task-4 503 path. CLI overrides keep precedence. Acceptance: config with only `[[backends.nodes]]` loads; startup log shows the effective backend.
- [ ] 70. **src/config — `deny_unknown_fields` sweep** [F-H2, D4]. Add to `AppConfig`, `ServerConfig`, `BackendConfig`, `BackendNodeConfig`, `BackendGroupConfig`, `StatsConfig`, `DumpConfig`, `SynthesisConfig` (the NEW struct from task 57), `InfluxDbConfig` (post-D2 field set), `RepromptConfig`, `AugmentBackendConfig`, TLS struct. EXCLUDED (oracle-corrected): `FixModuleConfig` (flatten), `StreamingMode`/`StreamingConfig` (unit-only enum alias — serde forbids deny on unit-only enums; alias removed by 73), `BackendsConfig` (HashMap alias `config/mod.rs:185` — no struct to annotate). Tests: one typo-rejection test per struct naming the bogus key; shipped `config.yaml.default` passes `check-config`.
- [ ] 71. **src/config/loader.rs — validation sweep** [F-M3, F-M4, F-M10, F-M16-adjacent, F-L2..L4, F-L10]. Host parses as `IpAddr` (bracket-tolerant) or RFC-1123 hostname; `augment.url` valid http(s); `reprompt.prompt_file`/followup file must exist → error; `dump.enabled` + empty path → error; empty-string sentinel rejected where non-empty required; `temperature` finite and in `0..=2`; duplicate `models → group` mappings across groups rejected; >1 `catch_all` group rejected.
- [ ] 72. **src/main.rs — validate AFTER CLI overrides** [F-M1, F-M2]. `fn validate_final(cfg: &AppConfig) -> Result<()>` (port ≠ 0, backend host non-empty, `--backend-url` parseable, augment url valid) called in `run_proxy` post-merge. `--port 0` rejected with an explicit message (ephemeral bind out of scope).
- [ ] 73. **Delete dead config surface** [D2, F-M5, F-M6, F-L1, F-L3..L6]. Remove: `stats.log_interval` (no consumer), `DetectionConfig`/`detection:` block, `StreamingConfig` legacy alias (migrate its test), `load_or_default()` + test, `AugmentBackendConfig::default`, `extract_user_content`. `FixModuleConfig.enabled` gets `#[serde(default = "always_true")]`. `config.yaml.default` updated to match exactly.
- [ ] 74. **src/proxy/server.rs:208 — CORS allow-list option** [C-L3]. `server.allowed_origins: Option<Vec<String>>`: `Some(list)` → `AllowOrigin::list`, `None` → current permissive default (yaml comment documents it). Acceptance: request from non-listed origin → no ACAO echo; listed origin → exact echo; `None` → header behavior byte-identical to today.

### Phase 13 — CLI, logging, shutdown — closes [F-M8, F-M9, F-H4, C-M11, F-L11..L14]

- [ ] 75. **src/main.rs:246-359, 427-446 — test-backend honesty** [F-M8, F-L11, F-M9, F-L14]. Per-node results collected; any failure → `Err` → exit code 1 (the single `process::exit` stays in `main` dispatch); `list-fixes` honors `--config` + real enabled state (see 32/34); version string `env!("CARGO_PKG_VERSION")`.
- [ ] 76. **src/main.rs — log ordering + env fallback** [F-L12, F-L15]. "Loading configuration from" trace moves to the real load site; post-merge line names the source + active CLI overrides; `RUST_LOG` fallback emits a one-line stderr `warn!`.
- [ ] 77. **src/main.rs:360-437 — dedupe** [F-L13]. `render_config_summary(&AppConfig)` shared by `log_config_settings`/`test_backend`; `probe_one_node(&BackendConfig)` shared by both probe loops; `run_proxy` split into `build_state() -> ProxyState` + `serve(state)`.
- [ ] 78. **src/proxy/server.rs — graceful shutdown** [C-M11, F-H4, D11].
  ```rust
  let listener = TcpListener::bind(bind_addr).await?;
  let (tx, rx) = tokio::sync::oneshot::channel::<()>();
  // spawn: ctrl_c -> send; cfg(unix) also SIGTERM via tokio::signal::unix
  axum::serve(listener, app).with_graceful_shutdown(async { let _ = rx.await; }).await?;
  exporters.flush_all().await; exporters.shutdown_all().await; // AFTER drain
  ```
  Acceptance (agent-executed): in-flight streaming request + SIGTERM → client stream completes intact, exporter flush logged before exit 0. Oracle-verified feasibility: axum 0.7 (`Cargo.toml:14`) `Serve::with_graceful_shutdown` matches the current `serve(listener, app)` form at `server.rs:217`; tokio `features=["full"]` includes `signal`; **clone the `Arc<ExporterManager>` (`server.rs:30`) into the shutdown path before `serve`** so flush-after-drain owns a handle.
- [ ] 79. **src/proxy/server.rs:212 — IPv6 bind** [C-L2]. Parse `server.host` into `IpAddr`, bind via `SocketAddr::from((ip, port))`; bind errors propagate (no unwrap on display).

### Phase 14 — Exporters — closes [A-M8, A-M9, A-M10, A-L8]

- [ ] 80. **src/exporters/mod.rs:43-50 + influxdb.rs:148-152 — feature gate hard error** [A-M9, D3]. `enabled: true` without the cargo feature → `ExporterManager::from_config` returns `Err("influxdb enabled in config but binary built without the influxdb feature")`. Test gated `#[cfg(not(feature = "influxdb"))]`.
- [ ] 81. **src/exporters/influxdb.rs:68-146 + mod.rs:119-134 — bounded queue + retry worker** [A-M8, A-M10]. `InfluxDbExporter { tx: mpsc::Sender<RequestMetrics>, worker: Mutex<Option<JoinHandle<()>>> }` (oracle: trait is `&self` and `JoinHandle::join` consumes → take-and-join under the Mutex; `new()` spawns a bounded (const 1024) worker; `export()` = non-blocking `try_send` (full → drop + counter + `debug!` — kills handler.rs:996 unbounded spawn; post-shutdown → `Err(Other("exporter shut down"))`); on `tx` close the worker DRAINS the remaining queue before exit; worker writes with 3 retries (100/500/2000 ms) and the client request timeout (task 83); `flush` drains, `shutdown` closes + joins. Acceptance: dead endpoint test — fixed spawn count, retries observed, clean join, post-shutdown export errors cleanly.
- [ ] 82. **src/exporters/mod.rs:104-134 — Arc registry: VERIFY-ONLY** [A-M10b]. Oracle-verified already satisfied: `Vec<Arc<dyn MetricsExporter>>` (`mod.rs:104`), `flush_all`/`shutdown_all` already `&self` (`:136`, `:148`). No change unless task 81's interior mutability forces a tweak; close the finding with a note in the commit body.
- [ ] 83. **src/exporters/influxdb.rs:131 — timestamp fallback + client timeout** [A-L8, E-M7-adjacent]. Unrepresentable timestamp → skip point + `debug!` (never epoch 0); client request timeout 10 s via influxdb2 client options.
- [ ] 84. **influxdb batch keys removal (completes D2)** [F-M7, A-M8]. Remove `batch_size`/`flush_interval_seconds` from `InfluxDbConfig`, defaults, `config.yaml.default`, and README's batching paragraph → "single write per request through a bounded queue". ONE commit with the code removal so nothing dangles.

### Phase 15 — Preflight & grouped balancing — closes [E-M2..M6, E-L6..L11]

- [ ] 85. **src/backends/preflight.rs:61-77, 238-242 — reuse the node client factory** [E-M5, E-M3, E-M7]. Probe clients from `node::build_client(cfg)` (TLS + timeouts, task 19); `cache_context_from_preflight` fetches `/props` with the node's auth builder (currently bare `Client::new()` — auth'd backends 401 silently).
- [ ] 86. **src/backends/preflight.rs — shared URL helper + capped fetch + model match + concurrent probes** [E-L6, E-L7, E-L8, E-L11, E-M4]. `pub(crate) fn node_url(base,&path,prefix)->String` used by all four strip sites (incl. context.rs post-15 — ONE implementation); `/models` fetch capped at 2 MiB (bytes → cap → parse), full payload at `debug!` + summary `info!`; `max_model_len` = entry whose `id` longest-matches `model_name`, fallback = max over entries (not first). E-M4: collect per-node probe futures and await via `futures::future::join_all`, applying results after — a 3-slow-node preflight takes ~max(t), not Σ(t); test with 3 fake backends of staggered delay.
- [ ] 87. **src/backends/grouped.rs:44-104 — deterministic order + catch-all fallback** [E-M2, E-M6]. Groups inserted sorted by name (stable `model→group` resolution); loader (71) rejects duplicate mappings; no handling group → `catch_all` if present, else FIRST SORTED group with `warn!` (per-group `exclusive: bool` default false to opt out). Acceptance: duplicate mapping fails check-config; no-catch-all config routes + warns.
- [ ] 88. **src/backends/mod.rs:30-38 + grouped.rs:21-23 — typed strategy + empty guard** [E-L9, E-L10]. `enum BalancingStrategy { #[default] PriorityFree, RoundRobin }` (Deserialize keeps kebab-case "priority-free"); empty `groups` → config-validation error (loader), not select-time panic.

### Phase 16 — Stats, logging, handler cleanup, docs — closes [A-M11, A-L6, A-L7, A-L10, C-L7, C-L9..L14, C-M9, F-M16]

- [ ] 89. **src/stats/collector.rs:428-435 — slot types + overflow** [A-L6]. `slot_id: u64`; context-total math via `saturating_add/sub`. Acceptance: collector tests green; `u64::MAX` fixture exercises the saturating path without panic.
- [ ] 90. **src/stats/collector.rs:164 — absence is not zero** [A-L7]. `completion_tokens`/`prompt_tokens` become `Option<u64>`; `from_response` sets `None` when usage lacks them; formatter prints `n/a`; InfluxDB point omits the field when `None`. Oracle ripple (must-do in same commit): update `has_throughput_signal` (`collector.rs:378` — recited round 2; also the `== 0` timings fallback at :202-205) and the exporter skip filter `log_sample_and_should_export` (`exporters/mod.rs:35`) — `None` IS exactly the README's documented skip condition, behavior preserved, types aligned.
- [ ] 91. **src/stats/collector.rs:135-349 — split `from_response`** [A-M11]. `extract_usage`, `extract_timings`, `extract_length_fallbacks`, `extract_vllm_usage`; assembler < 60 LOC. Pure move; collector tests green.
- [ ] 92. **src/stats/formatter.rs — split + display width** [A-L10, D12]. `format_pretty` → four section fns; padding via `unicode_width::UnicodeWidthStr::width` (add dep, the only one); constants renamed `TRIGGER_THRESHOLD_BYTES` / `HEAD_CHARS` / `TAIL_CHARS`.
- [ ] 93. **src/proxy/handler.rs + context.rs — residual LOW bundle** [C-L7, C-L9, C-L10, C-L12, C-L14]. `is_json_content_type` strips `;` params; the five `parse(...).unwrap_or_default()` sites get `unwrap_or_else(|e| { debug!(..); default })` incl. augment skip-logging when injection silently bypassed; request-path `unwrap()` sites (`Request::builder()`/`Method`/`.body()` at :350, :588, :1153, :1192, :1253 — [C-L7], momus-flagged) → `expect("static, built once")` or propagate; delete redundant request clones (:520-553, :635-636 — reuse the parsed value); delete dead `BackendType` enum; concurrent-gauge semantics doc-comment.
- [ ] 94. **src/proxy/context.rs:167-171 — staleness policy** [C-M9]. On `try_write` contention: serve stale, increment a `context_cache_stale_skips` counter surfaced in the stats line, `debug!`; comment states the accepted window (until one successful refresh).
- [ ] 95. **src/proxy/handler.rs — god-function split** [C-L11]. `handle_request` → `prepare_request` (parse/augment/overrides) + `route` (passthrough/monitoring/synthesis decision) + `emit_metrics` (stats/dump/log/export). Behavior-preserving moves; existing handler tests are the harness; split at the listed seam only.
- [ ] 96. **Test hygiene** [F-M16, C-L13]. `test_load_config_with_custom_paths` → `tempfile` path param, no cwd mutation; `ContextManager` gains constructor-injected `base_path: PathBuf` (prod passes `"."`) so global-cache tests isolate.
- [ ] 97. **Docs sync** [A note, D2, D10, F-M7]. `config.yaml.default` matches final schema; README: influx paragraph (task 84 wording), fake-mode delay table, `AGENTS.md` stale `from_streaming_chunks` → `from_synth_stream`. No other prose edits anywhere.

### Phase 17 — Sweep gates

- [ ] 98. **Dead-code + lint sweep.** Build with `dead_code` denied; remove everything the compiler now flags that this plan orphaned (superseded helpers from 23/53/67/86). No new `#[allow(dead_code)]`.
- [ ] 99. **Findings-to-tasks coverage audit.** Walk every finding in the six saved reports against Appendix B; each maps to a completed task or an Appendix A verified-clean entry; zero gaps before the F-wave. Fix any gap by reopening the mapped task.

## Final verification wave

- [ ] F1. Build matrix: `cargo build`, `--all-features`, `--no-default-features` — zero warnings; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo fmt --check` clean. Evidence: `.omo/evidence/big-fix/build.txt`.
- [ ] F2. Full tests: `cargo test --all-features` green; record test count before (baseline captured on first worker commit) and after — after ≥ before + new tests. Evidence: `.omo/evidence/big-fix/test.txt`.
- [ ] F3. E2E happy paths (backend `http://cosmo.lan:8700` if up, else a scripted fake): non-stream, stream, tool-call requests via `/v1/chat/completions` — responses validate against OpenAI shapes; stats log line prints and truncates CJK safely.
- [ ] F4. Multibyte stress: 2 KB CJK+emoji user message → no panic anywhere in the stats path; log truncation output is valid UTF-8 with boundaries respected.
- [ ] F5. Error-status matrix via curl (`-w '%{http_code}'` + body check): no backends → 503 JSON; invalid JSON → 400 JSON; wrong media type → 415 JSON; dead augment URL → 502 JSON. Envelope shape asserted.
- [ ] F6. Pass-through preservation: unknown top-level field + unknown message-level field + unknown Anthropic content block → all present at the (fake) backend; unknown response field → present at the client.
- [ ] F7. Config gates: `check-config` rejects a typo'd key (error names the key), rejects two catch-all groups and duplicate model mappings, rejects enabled-dump-with-empty-path, accepts shipped `config.yaml.default`.
- [ ] F8. Balancer: live two-node test with one node 502-flapping → cooldown skips it; task-17 concurrency test green; guard-count returns to 0 after streams end.
- [ ] F9. Graceful shutdown: streaming in flight → SIGTERM → client stream intact, exit 0, flush log AFTER drain (ordering asserted from the log).
- [ ] F10. Reprompt e2e: sentinel-bearing first response from fake backend → follow-up fires; exactly ONE stats line with merged totals; fixes applied to the merged body; budget test returns within `max_total_ms`.
- [ ] F11. Feature matrix: F3 subset with `--no-default-features --features rustls-tls` proves D3 hard-error path and feature independence.
- [ ] F12. Compatibility checklist (AGENTS.md items 1-6) re-run against final HEAD; Appendix B map complete, zero unmapped findings; dual-reviewer (neckbeard + hoodie) pass recorded for the whole branch.

## Commit order (authoritative) & dependency matrix

Task numbers are IDs, not sequence. One commit per task row; the commit sequence is:

**100 → 1…21 → 31 → 22 → 23 → 24 → 25 → 26 → 27 → 28 → 29 → 30 → 32…56 → 57…77 → 78 → 79 → 80 → 83 → 81 → 84 → 82 → 85…97 → 98 → 99 → F1…F12** (ellipsis = numeric ascending; 78 explicit because it sits inside the re-ordered 78–84 cluster).

Constraint rationale: 1→2→3 char-safety shared first. 13 before 14 (gate rides the routed path). 31 before 22/26/27/29 (Result-based interface lands first). 23 before 24–27 (scanner). 33 AFTER 29 (its doc describes the post-29 removal state — "29 after 33" in earlier drafts was inverted, round-2 fix); 34 finalized in 75. 43–48 independent of 35–41 except tests. 49→50→51–56 before 57–62. 63–68 after 49–56. 57 before 70/73 (SynthesisConfig exists before the deny-sweep; alias removal after). 69–74 before 78–84 (config schema frozen first). 83 before 81 (worker uses the client timeout). 81 before 84: batch fields are ALREADY dead (oracle round 2: reads only in startup log `main.rs:382-383`, a test literal, and struct assignment — no batching logic), so 84 compiles standalone whenever; it lands after 81 so the README "single write through a bounded queue" sentence is true on arrival. 82 verify-only; any tweak forced by 81's interior mutability rides commit 81. 85–88 after 19–20. 89–97 last. 98→99→F-wave; F-wave runs only with every implementation row checked.

## Appendix A — Verified-clean (do NOT "fix" these; audit explicitly confirmed)

SSE chunk framing construction; buffered accumulator delta math vs streaming reference; no mutex-poisoning risk; no div-by-zero in rate calcs; stats accounting invariants (`prompt + cached == completion`); `finish_reason` extraction across backends; cache-hit-rate math; `ChatMessage` role/tool_calls skip logic; `ToolCall.function` requiredness (spec-correct); heartbeat/keepalive timing; API-KEY masking in all log/display paths; TLS builder wiring (minus preflight reuse, fixed in 85); hop-by-hop filter correctness (until D6 consolidates it).

## Appendix B — Finding → task coverage map (by report)

Map order follows each report's FINAL-SUMMARY block, not its numbered detail list — e.g. [D] numbered item "2. MEDIUM :628" is M6 on this map (round-2 note).

- [A] C1→1; H1→39; M1→35; M2→36; M3→37; M4→37; M5→38; M6→40; M7→40; M8→81+83+84; M9→80; M10→81+82; M11→91; L1→36; L2→36; L3→35; L4→41; L5→41; L6→89; L7→90; L8→83; L9→42; L10→92.
- [B] H1→22; H2→2+3; H3→26; H4→23+24; M1→23+24; M2→25; M3→30; M4→26; M5→27; M6→28; M7→29; M8→29; L1→30; L2→30; L3→30+32; L4→30; L5→33; L6→29; L7→2; L8→29.
- [C] H1→4; H2→7; H3→9; H4→13; H5→14; M1→68; M2→63; M3→64; M4→8; M5→10; M6→66; M7→65; M8→15; M9→94; M10→12; M11→78; L1→16; L2→79; L3→74; L4→6; L5→5; L6→5; L7→93; L8→11; L9→93; L10→93; L11→95; L12→93; L13→96; L14→81; L15→93; L16→67; L17→63; L18→67; L19→65.
- [D] M1→52; M2→51; M3→53; M4→53; M5→53; M6→49; M7→50+54; M8→57; M9→61; M10→36; L1→56; L2→56; L3→55; L4→53; L5→53; L6→56; L7→55; L8→55; L9→55; L10→56; L11→56; L12→56; L13→55; L14→58; L15→59; L16→59; L17→62; L18→58; L19→60; L20→60; L21→62; L22→62.
- [E] H1→18; H2→17; M1→21; M2→87+71; M3→85; M4→86 (join_all); M5→85; M6→87; M7→19+85; L1→20; L2→20; L3→20; L4→20; L5→20; L6→86; L7→86; L8→86; L9→88; L10→88; L11→86.
- [F] H1→69; H2→70; H3→43; H4→78; M1→72; M2→72; M3→71; M4→71; M5→73; M6→73; M7→84; M8→75; M9→34+75; M10→71; M11→44; M12→45; M13→46; M14→47; M15→43; M16→96; L1→73; L2→71; L3→71; L4→73; L5→73; L6→73; L7→48; L8→48; L9→48; L10→71; L11→75; L12→76; L13→77; L14→75; L15→76.

## Appendix C — Rollback & risk

Each phase = one revertable commit; risky clusters isolated by design: deletions (29, 73, 84), interface change (31), permit semantics (18), shutdown (78). Highest behavioral-change-risk commits: 4 (status codes — clients previously seeing 200 now see 503; that IS the fix), 14 (injection gating), 57 (delay default 0). Mitigation for all three: F-wave e2e runs against a real backend before handoff. On any F-task failure: revert the specific phase commit, uncheck that task row, record the failure under the task line.
