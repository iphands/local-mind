# What ngram_embedding.py (this overlay) was derived from. Sourced by
# ./qwen3.8-flash-next/run, which mounts the overlay over UPSTREAM_PATH only when
# the image's own copy of that file hashes to BASE_SHA256 -- i.e. by the
# upstream file's CONTENT, not by vLLM commit, so a VLLM_MAIN_SHA bump that
# leaves this file alone needs no edit here. When the launcher refuses:
# re-derive the overlay from the new upstream file (keep the one marked
# "local-mind patch" branch in from_quant_config), regenerate
# ngram_embedding.diff, and update the lines below.
#
# UPSTREAM_PATH is relative to site-packages. from_quant_config moved from
# nvidia/ngram_embedding.py into common/ngram_embedding.py in vLLM 267eee5
# (vllm#57497, 2026-09-24); nvidia/ now imports the classes from ../common.
# BASE_REF is provenance only (the commit the overlay was last re-derived at).
UPSTREAM_PATH=vllm/models/qwen4_exp/common/ngram_embedding.py
BASE_REF=a44d7b51511fff6fde2bafb9c4715433a7627d1c
BASE_SHA256=1392f51ccf0b610542bf05c3021271bf6754221d9616ff92831b94f79fc3a892
